# pfed-c

An independent participant in the plankton federation. This repo holds only **my own** signed, content-addressed records (`plankton-data/`, `nekton-data/`). The cross-references to other participants resolve in the [federation union](https://github.com/gitmick/plankton-federation-demo).
